<% metric = ( job==:clade_finding ? "AAI" : "ANI" ) %>
<% unless res.file_path(:medoids).nil? %>
  <% n = File.foreach(res.file_path(:medoids)).count %>
  MiGA proposes
  <%= link_to(medoid_clade_path(project.id, metric, 0)) { %>
    <%= n %> <b><%= metric %></b> medoid clade<%= "s" if n!=1 %><% } %>.
<% end %>
