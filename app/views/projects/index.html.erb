<% provide(:title, "All projects") %>
<h1>Projects</h1>

<div class='alert alert-info text-center'>
  Click on the <i class='glyphicon glyphicon-info-sign text-info'> </i> icon
  next to a project's name for additional information
</div>

<%= will_paginate class: "center-block center" %>

<div class="project-list">
   <%= render @projects %>
</div>

<%= will_paginate class: "center-block center" %>

